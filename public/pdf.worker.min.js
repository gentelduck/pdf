// // public/service-worker.js
//
// const CACHE_NAME = "react-pdf-cache-v1";
// const ASSETS_TO_CACHE = [
//   "/",
//   "/index.html",
//   "/static/js/bundle.js",
//   "/static/js/0.chunk.js",
//   "/static/js/main.chunk.js",
//   "/pdf.worker.min.js", // Include your worker here if needed
// ];
//
// // Install event: Cache the required assets
// self.addEventListener("install", (event) => {
//   event.waitUntil(
//     caches.open(CACHE_NAME).then((cache) => {
//       return cache.addAll(ASSETS_TO_CACHE);
//     })
//   );
// });
//
// // Activate event: Clean up old caches
// self.addEventListener("activate", (event) => {
//   const cacheWhitelist = [CACHE_NAME];
//   event.waitUntil(
//     caches.keys().then((cacheNames) => {
//       return Promise.all(
//         cacheNames.map((cacheName) => {
//           if (!cacheWhitelist.includes(cacheName)) {
//             return caches.delete(cacheName);
//           }
//         })
//       );
//     })
//   );
// });
//
// // Fetch event: Intercept network requests and serve cached responses
// self.addEventListener("fetch", (event) => {
//   event.respondWith(
//     caches.match(event.request).then((cachedResponse) => {
//       // Return the cached response if it exists, or fetch from the network
//       return cachedResponse || fetch(event.request);
//     })
//   );
// });
